{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.Gliana-Labs/gliana-ai-mcp",
  "title": "GlianaAI",
  "description": "Pay-per-call AI: 90+ generative models (image, video, music, speech, LLM chat), plus market data and SEC filings. No signup, no API key — each call settles as a USDC micropayment from your own wallet.",
  "repository": {
    "url": "https://github.com/Gliana-Labs/gliana-mcp",
    "source": "github"
  },
  "version": "0.5.3",
  "packages": [
    {
      "registryType": "npm",
      "registryBaseUrl": "https://registry.npmjs.org",
      "identifier": "gliana-ai-mcp",
      "version": "0.5.3",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "GLIANA_WALLET_KEY_FILE",
          "description": "Path to a file (chmod 600) holding a 0x EVM private key for a wallet with USDC on Base/Tempo. Preferred over GLIANA_WALLET_KEY: your MCP client config gets synced, committed and screenshotted; a key file does not. Use a dedicated wallet holding only what you intend to spend. Discovery tools work with no key at all.",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "GLIANA_WALLET_KEY",
          "description": "0x EVM private key, inline. Works, but prefer GLIANA_WALLET_KEY_FILE — an inline key lives in your MCP client config. Discovery tools work without it.",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "GLIANA_SOLANA_KEY_FILE",
          "description": "Path to a file (chmod 600) holding a Solana secret key. Preferred over GLIANA_SOLANA_KEY.",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "GLIANA_SOLANA_KEY",
          "description": "Solana secret key (base58 or JSON array), inline. Prefer GLIANA_SOLANA_KEY_FILE.",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "GLIANA_RAIL",
          "description": "Payment rail for generate: base | tempo | solana (default base).",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "GLIANA_API_URL",
          "description": "Override the gateway base URL (default https://api.glianalabs.com).",
          "isRequired": false,
          "isSecret": false
        }
      ]
    }
  ]
}
